package-as: RangeFinder_Classic

manual-changelog:
    filename: CHANGELOG.md
    markup-type: markdown

ignore:
    # Folders
    - dev-tools
    # Dev Files
    - .env.local
    - .gitignore
    - bun.lockb
    # JSON
    - package.json
    - tsconfig.json